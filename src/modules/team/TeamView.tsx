import { FC } from 'react';

import { DashboardLayout } from '../../shared/components/ui/layouts/DashboardLayout';

/**
 *
 * @returns {JSX.Element} - Team View.
 */
export const TeamView: FC = () => (
  <DashboardLayout>Team content</DashboardLayout>
);
